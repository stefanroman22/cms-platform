# Runtime dependencies for the FastAPI backend. Vercel's @vercel/python
# builder reads this file from backend/ and installs every entry. The
# auth_service app is served via vercel_entry.py.
#
# Legacy Django entries were dropped in the env-config-hygiene pass
# (Pass C). If you need Django for archaeology, check git history.
#
# CI-004 / DEP-009 — CI installs from `requirements.lock` (generated
# via `pip-compile --generate-hashes`) using `--require-hashes`.
# Vercel cannot use lockfiles, so this file is the source of truth for
# top-level pins. Whenever you edit this file, regenerate the lock:
#   cd backend && pip-compile --generate-hashes \
#     --output-file=requirements.lock requirements.txt

fastapi==0.136.1
starlette==0.49.1
uvicorn==0.47.0
# uvicorn[standard] dropped — its `uvloop` transitive is Linux-only and
# pip-compile (which we run on Windows for CI-004 / DEP-009 hash pinning)
# can't generate a hash entry for a wheel that doesn't exist on this
# platform. Vercel uses its own ASGI runner via `vercel_entry.py` so the
# perf extras only matter for `uvicorn auth_service.main:app` local dev,
# and the default asyncio loop is fine there.
argon2-cffi==23.1.0
supabase==2.30.0
pydantic[email]==2.13.4
pydantic-settings==2.14.1
slowapi==0.1.9
python-dotenv==1.2.2
python-multipart==0.0.29
resend==2.30.1
